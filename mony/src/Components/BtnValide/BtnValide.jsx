import { useNavigate } from "react-router-dom";
import '../CommandeMenuBtn/CommandeMenuBtn.css'; 

const AjoutPanierBtn = (props) => {
    const ajoutPanier = () =>{
    }
    return(
        <button className="btn-commande">Valider ma commande</button>
    )
}

export default AjoutPanierBtn;