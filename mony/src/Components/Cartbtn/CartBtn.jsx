//import './btn.css';
import { useNavigate } from "react-router-dom";
//import AppRouter from '../../router/AppRouter';

const CartButton = (props) => {
    const commandebtn = () =>{
    }
    return(
        <button className="btn-commande" >Ajouter au panier</button>
    )
}

export default CartButton;